# Architecture report assets

This folder contains the updated presentation architecture diagram.

The diagram keeps the layered structure from the original architecture report but updates the content to match the current DSCI601 workspace implementation:

- clinical evaluator, runner, environment, allocator, and model registry
- EQUITAS mediation and fairness-state generation
- preserved quantum-routing workflow and saved-state analysis path
- fairness-performance synthesis and reportable artifacts
- next-semester active iCMAB and fairness-feedback extension points

Use `architecture_diagram.tex` inside Beamer with `\input{...}` and scale it with `\resizebox`.
